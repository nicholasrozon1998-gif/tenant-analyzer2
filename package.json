{
  "name": "ai-tenant-analyzer",
  "version": "2.0.0",
  "private": true,
  "description": "AI Tenant Analyzer — document-driven tenant screening with automatic extraction and deterministic scoring.",
  "engines": { "node": ">=18.17" },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
